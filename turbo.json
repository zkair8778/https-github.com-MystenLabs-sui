{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["build/**", "dist/**"]
    }
  },
  "globalEnv": [
    "VITE_NETWORK",
    "VITE_VERCEL_ANALYTICS_ID"
  ]
}
